---
name: smoke-tests
description: Use before any release, and when changing the build, the export map or the package manifest. Covers the dist-level smoke suite and the packaging audit the unit tests structurally cannot perform. Scope: The dist-level smoke suite and the packaging audit: export map, dual module output, shared chunk identity
---

# smoke-tests

<!-- generated by scripts/sync-claude-skills.mjs -- do not edit -->

The instructions for this skill live in `.agents/skills/smoke_tests/SKILL.md`, which is also what the Antigravity
pipeline loads under the registry name `smoke_tests`. This file only points at them so both
toolchains obey one text.

**Read `.agents/skills/smoke_tests/SKILL.md` in full and follow it.** Do not act on this summary.
