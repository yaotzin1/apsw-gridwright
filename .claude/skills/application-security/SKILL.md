---
name: application-security
description: Use before writing or reviewing any code that renders, serializes, fetches, parses, serves or extends anything - components, add-ons, exporters, data sources, the playground and the scripts. Covers the exploit classes a data grid actually has, the banned APIs, and the safe patterns that replace them. Blocking - there are no exceptions. Scope: Exploit classes a grid has: HTML and script sinks, injection into files, URLs, selectors and styles, sandboxed documents, prototype pollution, ReDoS, what add-ons may reach, the development server. Banned APIs, no exceptions
---

# application-security

<!-- generated by scripts/sync-claude-skills.mjs -- do not edit -->

The instructions for this skill live in `.agents/skills/application_security/SKILL.md`, which is also what the Antigravity
pipeline loads under the registry name `application_security`. This file only points at them so both
toolchains obey one text.

**Read `.agents/skills/application_security/SKILL.md` in full and follow it.** Do not act on this summary.
