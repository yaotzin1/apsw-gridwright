---
name: styling
description: Use when touching CSS, class names, or any user-visible string. Covers the unstyled contract, CSS custom properties, dark mode, and why every label lives in one object. Scope: CSS custom properties, class name overrides, dark mode, reduced motion, every visible string in labels
---

# styling

<!-- generated by scripts/sync-claude-skills.mjs -- do not edit -->

The instructions for this skill live in `.agents/skills/styling/SKILL.md`, which is also what the Antigravity
pipeline loads under the registry name `styling`. This file only points at them so both
toolchains obey one text.

**Read `.agents/skills/styling/SKILL.md` in full and follow it.** Do not act on this summary.
