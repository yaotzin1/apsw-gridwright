---
name: performance
description: Use when touching the pipeline, the row build, or anything that runs per row or per render. Covers allocation discipline, large data sets, and measuring before changing. Scope: Per-row work, pipeline allocation, render counts, large data sets, measuring before changing
---

# performance

<!-- generated by scripts/sync-claude-skills.mjs -- do not edit -->

The instructions for this skill live in `.agents/skills/performance/SKILL.md`, which is also what the Antigravity
pipeline loads under the registry name `performance`. This file only points at them so both
toolchains obey one text.

**Read `.agents/skills/performance/SKILL.md` in full and follow it.** Do not act on this summary.
