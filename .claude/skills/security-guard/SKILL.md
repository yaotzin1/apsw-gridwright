---
name: security-guard
description: Use when adding a dependency, changing what the tarball contains, or touching the lockfile, install scripts or the publish gate. Covers supply chain policy and publish safety. Exploit classes in code - XSS, injection, sandboxing, prototype pollution, the dev server - are in application_security. Scope: Runtime dependency policy, install scripts, the lockfile and .npmrc, what must never enter the tarball
---

# security-guard

<!-- generated by scripts/sync-claude-skills.mjs -- do not edit -->

The instructions for this skill live in `.agents/skills/security_guard/SKILL.md`, which is also what the Antigravity
pipeline loads under the registry name `security_guard`. This file only points at them so both
toolchains obey one text.

**Read `.agents/skills/security_guard/SKILL.md` in full and follow it.** Do not act on this summary.
