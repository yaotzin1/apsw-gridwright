import type { AddonMessages } from '../../i18n/messages';

export const ROW_ACTIONS_ADDON = 'gridwright:row-actions';
export const INLINE_EDITING_ADDON = 'gridwright:inline-editing';

/** English. The translations live in `apsw-gridwright/locales`, under `addons['gridwright:row-actions']`. */
export const rowActionsMessages: AddonMessages = {
    en: {
        // The menu's accessible name, so a reader knows which menu opened.
        menu: 'Row actions',
    },
};
